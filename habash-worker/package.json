{
  "name": "habash-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute habash-db --file=src/schema.sql --remote",
    "schema:local": "wrangler d1 execute habash-db --file=src/schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.0.0"
  }
}
